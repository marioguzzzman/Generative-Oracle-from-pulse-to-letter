void tragedia () {

  //- .-. .- --. . -.. .. .- .-.-. TRAGEDIA
  if(estado ){
    linea();
  } 
  if(estado ){
    punto();
  }
  if(estado ){
    linea();
  }   
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }

  if(estado ){
    linea();
  }
  if(estado ){
    linea();
  }
  if(estado ){
    linea();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }

  if(estado ){
    linea();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    punto();
  }
  if(estado ){
    linea();
  }   
  if(estado ){
    punto();
  }
  if(estado ){
    linea();
  }   
  if(estado ){
    punto();
  } 
  if(estado ){
    linea();
  }   
  if(estado ){
    punto();
  } 
  if(estado ){
    linea();
  }   
  if(estado ){
    punto();
  }
}



